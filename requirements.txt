# requirements.txt
streamlit>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0
PyPDF2>=3.0.0
python-docx>=0.8.11
schedule>=1.1.0

# LangChain and LLM providers
# Updated for LangChain 1.0+ compatibility
langchain>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0
langchain-google-genai>=3.0.0,<4.0.0
# Note: google-generativeai is optional and only used as fallback
# It's not included here to avoid dependency conflicts with langchain-google-genai
# Install separately if needed: pip install google-generativeai
langchain-community>=0.3.0,<1.0.0
langchain-core>=1.0.0,<2.0.0
langchain-text-splitters>=0.3.0,<1.0.0

# Vector stores and embeddings
chromadb>=0.4.22
langchain-chroma>=0.1.0  # Preferred over deprecated langchain_community.vectorstores.Chroma
tiktoken>=0.5.2
# Note: sentence-transformers removed - using cloud-based embeddings for GitHub Actions compatibility
# Install with: pip install sentence-transformers (only if you need local embeddings)

# GitHub API
PyGithub>=2.1.1

# Additional utilities
gitpython>=3.1.40
numpy>=1.24.0
